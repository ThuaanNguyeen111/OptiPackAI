# ==============================
# Root .env - chỉ dùng cho docker-compose ở mức root
# Mỗi workspace (be/, fe/) có .env riêng, xem be/.env.example và fe/.env.example
# ==============================

# MongoDB (dev, chạy qua docker-compose)
MONGO_INITDB_ROOT_USERNAME=admin
MONGO_INITDB_ROOT_PASSWORD=admin123
MONGO_EXPRESS_PORT=8081
